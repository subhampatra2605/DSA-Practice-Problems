**LeetCode 67 – Add Binary**

# 🧠 Problem Type

* **Category:** String
* **Pattern:** Two Pointers + Carry Simulation
* **Concept:** Manual binary addition (right → left)

---

# 🎯 Core Idea

Add like normal addition:

```
bit1 + bit2 + carry
```

* Result bit = `sum % 2`
* New carry = `sum / 2`

Traverse from **end of both strings**.

---

# 🔁 Algorithm Steps

1. Initialize:

   * `i = a.length() - 1`
   * `j = b.length() - 1`
   * `carry = 0`
   * `StringBuilder result`

2. Loop while:

   ```
   i >= 0 OR j >= 0 OR carry != 0
   ```

3. For each iteration:

   * `sum = carry`
   * If `i >= 0` → add `a.charAt(i) - '0'`
   * If `j >= 0` → add `b.charAt(j) - '0'`
   * Append `sum % 2`
   * Update `carry = sum / 2`

4. Reverse result and return.

---

# ⚡ Important Edge Cases

* Different lengths
  Example: `"11"` + `"1"`

* Final carry left
  Example: `"1"` + `"1"` → `"10"`

* Both strings `"0"`

---

# ⏱ Complexity

* **Time:** O(max(n, m))
* **Space:** O(max(n, m))

---

# 🧩 Why Reverse at End?

We append from right → left
So we must reverse before returning.

---

# 🧠 Pattern Recognition

This same logic is used in:

* Add Strings (Decimal version)
* Add Two Numbers (Linked List)
* Big Integer addition
* Multiply Strings (extended version)
